name: glitter_touch_land
description: A high-performance Flutter glitter jar simulation.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^6.1.1
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  vector_math: ^2.1.4
  get_it: ^7.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
